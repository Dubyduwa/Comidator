<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>APP COMIDAS</title>
  <link rel="stylesheet" href="https://bootswatch.com/4/minty/bootstrap.min.css">
</head>
<body background="https://www.cocinavital.mx/wp-content/uploads/2018/02/platillos_para_una_comida_de_cuaresma_portada.jpg">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/profile">APP COMIDAS</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  </nav>
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-5">
        <div class="card text-dark border-primary">
          <div class="card-header">
            <h4><%= data.titulo%></h4>
          </div>
          <div class="card-body">
            <form action="/buyoffer/<%= data.id %>" method="post">
              <img style="height: 200px; width: 100%; display: block;" src="<%= data.imagen %>" alt="Form image">
              <h1></h1>
              <label>Vendido por <%= data.vendedor %></label>
              <label>Quedan <%= data.porciones %> porciones de esta oferta, ¿Cuantas porciones quieres comprar?</label>
              <div class="form-group">
                <input type="text" name="porciones" placeholder="nº de porciones" class="form-control">
              </div>
              <input type="hidden" name="idusuario" value="<%= data.idusuario %>">
              <input type="hidden" name="vendedor" value="<%= data.vendedor %>">
              <button type="submit" class="btn btn-primary mt-2">
                Realizar la compra
              </button>
              <a href="/viewoffer/<%= data.id %>" class="btn btn-danger btn-sm mt-2">Volver</a>
            </form>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
