<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>APP COMIDAS</title>
  <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
</head>
<body>

  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/profile">APP COMIDAS</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
        <a class="nav-link" href="/updateuser/<%= user.id %>">Editar perfil</a>
      </li>
        <li class="nav-item">
          <a class="nav-link" href="/logout">Cerrar sesión</a>
        </li>
      </ul>
      <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" type="text" placeholder="Busca una oferta">
        <a href="#" class="btn btn-secondary">Buscar</a>
      </form>
    </div>
  </nav>

 <div class="container">
  <div class="page-header text-left mt-2">
   <h1>Hola <%= user.username %>, tus ofertas:</h1>
  </div>
 </div>

<div class="container">
  <div class="row">
    <div class="col-md-7">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <th>Oferta</th>
            <th>Descripción</th>
            <th>Porciones</th>
            <th>Localización</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
            <% if (data) { %>
              <% for (var i = 0; i < data.length; i++) { %>
                <% if (data[i].idusuario == user.id) { %>
                <tr>
                  <td> <%= data[i].titulo %> </td>
                  <td> <%= data[i].descripcion %> </td>
                  <td> <%= data[i].porciones %> </td>
                  <td> <%= data[i].ciudad %> </td>
                  <td>
                    <a href="/updateoffer/<%= data[i].id %>" class="btn btn-info"> Editar </a>
                    <a href="/deleteoffer/<%= data[i].id %>" class="btn btn-danger"> Borrar </a>
                  </td>
                </tr>
                <% } %>
              <% } %>
            <% } %>
        </tbody>
      </table>
    </div>
    <div class="col ml-5">
    <div class="card">
      <div class="card-header">
        <h4>Publicar una oferta</h4>
      </div>
      <div class="card-body">
        <form action="/addoffer" method="post">
          <div class="form-group">
            <input type="text" name="titulo" placeholder="Añade un título" class="form-control">
          </div>
          <div class="form-group">
            <input type="text" name="descripcion" placeholder="Añade una descripción" class="form-control">
          </div>
          <div class="form-group">
            <input type="int" name="porciones" placeholder="Añade número de porciones" class="form-control">
          </div>
          <div class="form-group">
            <input type="text" name="ciudad" placeholder="Añade una localización" class="form-control">
          </div>
          <div class="form-group">
              <select type="text" name="alergenos" class="form-control">
                <option selected disabled>¿Contiene alérgenos?</option>
                <option></option>
                <option>Lactosa</option>
                <option>Gluten</option>
                <option>Marisco</option>
                <option>Frutos secos</option>
                <option>Trigo</option>
                <option>Pescado</option>
                <option>Cacahuete</option>
                <option>Crustácetos</option>
                <option>Datsi</option>
              </select>
            </div>
          <button type="submit" class="btn btn-primary">
            Publicar oferta
          </button>
        </form>
      </div>
    </div>
    </div>
  </div>
</div>

<div class="container">
 <div class="page-header text-left mt-5">
  <h1>Ofertas que te pueden interesar:</h1>
 </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-md-7 mt-2">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <th>Oferta</th>
            <th>Vendedor</th>
            <th>Descripcion</th>
            <th>Porciones</th>
            <th>Localización</th>
            <th>Alérgenos</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
            <% if (data) { %>
              <% for (var i = 0; i < data.length; i++) { %>
                <% if (data[i].idusuario != user.id) { %>
                <tr>
                  <td> <%= data[i].titulo %> </td>
                  <td> <%= data[i].vendedor %> </td>
                  <td> <%= data[i].descripcion %> </td>
                  <td> <%= data[i].porciones %> </td>
                  <td> <%= data[i].ciudad %> </td>
                  <td> <%= data[i].alergenos %> </td>
                  <td>
                    <button type="button" class="btn btn-success"> Examinar </button>
                  </td>
                </tr>
                <% } %>
              <% } %>
            <% } %>
        </tbody>
      </table>
    </div>
    <div class="col md-4 ml-5">

    </div>
  </div>
</div>

 </div>
</body>
</html>
