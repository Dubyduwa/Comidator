<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>APP COMIDAS</title>
  <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
</head>
<body background="https://www.cocinavital.mx/wp-content/uploads/2018/02/platillos_para_una_comida_de_cuaresma_portada.jpg">

  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/profile">APP COMIDAS</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  </nav>



<span class="badge badge-pill badge-light ml-5 mt-3">
 <div class="container">
  <div class="page-header text-left mt-2">
   <h1>Ranking de vendedores: </h1>
  </div>
 </div>
</span>

<div class="container">
  <div class="row">
    <div class="col-md-7 mt-2">
      <table class="table table-bordered table-dark">
        <thead>
          <tr>
            <th>Posición</th>
            <th>Vendedor</th>
            <th>Nota</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
            <% if (data) { %>
              <% for (var i = 0; i < data.length; i++) { %>
                <tr>
                  <td> <%= (i + 1) %></td>
                  <td> <%= data[i].username %> </td>
                  <td> <%= data[i].nota %> </td>
                  <td>
                    <a href="/viewuserranking/<%= data[i].id %>" class="btn btn-success"> Ver perfil de <%= data[i].username %> </a>
                    <a href="/viewofferuser/<%= data[i].id %>" class="btn btn-success mt-1"> Examinar ofertas de <%= data[i].username %> </a>
                  </td>
                </tr>
                <% } %>
            <% } %>
        </tbody>
      </table>
    </div>
  </div>
  <a href="/profile" class="btn btn-danger btn-sm">Volver</a>
</div>

</body>
</html>
